node_modules/
dist/
*.log
capture-*.log
gcode/*.gcode
gcode/*.mapped.gcode

# local runtime state — never commit
config.json
printlog.json
queue.json
auth.json
tunnel.json
spools.json
cloudflared
cloudflared.exe

# v2.9 loadout state — never commit
slots.json

# v2.11: Dispatch state (never commit — schedules, deadlines, job history)
dispatch.json

# v2.16: Resource Monitor state (spool inventory, prices, buy links, colour map)
resources.json

# v2.18: update-check cache — last check time, last seen release, dismissal.
# NOTE the singular update.json at the repo root IS committed: that is the
# published release manifest every Hub fetches. This one, plural-stem and
# hyphenated, is per-install state. Do not confuse them.
update-state.json
update-state.json.tmp

# State-file backups and the stranded temp files that led to them (2026-09-01).
# These hold real scheduling data — never commit them, never delete them either.
dispatch.json.tmp
*.json.bak-*
*.json.tmp-*

# local debris that should never ride along (v2.11 sweep)
*.exe
bridge.log
capture-*.log
run_bridge.cmd
sf3d_hub_bridge.py
claude/
after-color.json
baseline.json
db-list.json
fp-after.json
fp-before.json
gstore*.json
u3-check.json
fix-v*-versions.txt
test-out.txt
slicejobs.json

slicejobs.json
access.log

# v2.23: extracted-thumbnail cache (rebuilt from the gcode folder on demand)
thumbs/
