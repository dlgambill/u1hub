node_modules/
dist/
*.log
capture-*.log
gcode/*.gcode
gcode/*.mapped.gcode

# local runtime state — never commit
config.json
printlog.json
queue.json
auth.json
tunnel.json
spools.json
cloudflared
cloudflared.exe

# v2.9 loadout state — never commit
slots.json
